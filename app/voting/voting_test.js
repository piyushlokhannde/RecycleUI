'use strict';

describe('myApp.voting module', function() {

  beforeEach(module('myApp.voting'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var votingCtrl = $controller('votingCtrl');
      expect(votingCtrl).toBeDefined();
    }));

  });
});